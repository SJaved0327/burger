<li>
	<p>
		{{burger_name}}
	</p>

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}YUMMY!!{{else}}Can't wait to eat this!{{/if}}
	</button>

</li>
